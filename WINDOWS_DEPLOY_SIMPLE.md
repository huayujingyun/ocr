# 🚀 Windows本地部署 - 最简单方案

## ⭐ 推荐方案：标准部署（无需Docker）

**为什么推荐？**
- ✅ 无需安装Docker
- ✅ 自动安装Python和Node.js
- ✅ 一键启动
- ✅ 适合大多数Windows用户

**时间**：10分钟
**难度**：⭐⭐

---

## 📋 准备工作

### 前提条件
- Windows 10/11 64位
- 4GB+ 内存
- 2GB+ 可用磁盘空间
- 管理员权限

### 检查系统
- 按 `Win + R`，输入 `winver`，查看Windows版本
- 确认是Windows 10/11 64位

---

## 🎯 3步完成部署

### 步骤1：下载部署包

**方案A：从GitHub下载（推荐）**

1. 访问：https://github.com/huayujingyun/ocr/releases
2. 下载最新版本的部署包：`ocr-card-recognizer-windows-v2.0.0.zip`
3. 解压到任意目录（推荐：`C:\OCR\`）

**方案B：从项目目录打包（如果没有发布版本）**

在项目根目录执行：
```powershell
.\scripts\build-windows-package.ps1
```
生成的文件：`ocr-card-recognizer-windows-v2.0.0.zip`

---

### 步骤2：双击安装

1. 右键点击 `install.bat`
2. 选择 **"以管理员身份运行"**

**安装内容**：
- ✅ 自动下载并安装Python 3.12（如果未安装）
- ✅ 自动下载并安装Node.js（如果未安装）
- ✅ 安装后端依赖（PaddleOCR、FastAPI等）
- ✅ 安装前端依赖（Next.js、React等）
- ✅ 下载OCR模型文件（约200MB）

**等待时间**：
- 首次安装：5-10分钟（取决于网速）
- 后续安装：2-3分钟

**安装成功提示**：
```
========================================
安装完成！
========================================
现在可以双击 start.bat 启动服务
访问地址：http://localhost:5000
========================================
```

---

### 步骤3：启动服务

1. 双击 `start.bat`（无需管理员权限）
2. 等待服务启动（首次启动约30-60秒）
3. 看到以下提示表示启动成功：

```
========================================
服务已启动！
========================================
前端地址：http://localhost:5000
后端地址：http://localhost:8001
========================================
```

4. 打开浏览器访问：http://localhost:5000

✅ **完成！** 现在可以开始使用OCR识别功能了

---

## 🎮 使用指南

### 1. 上传图片

- 点击 **"上传图片"** 按钮
- 选择购物卡/加油卡图片
- 支持多张图片同时上传（最多10张）

### 2. 识别卡片

**方法A：自动识别（推荐新手）**
- 直接点击 **"开始识别"** 按钮
- 系统自动识别卡号和密码

**方法B：模板识别（推荐，更准确）**
1. 点击 **"设置识别模板"**
2. 上传一张标准卡片图片
3. 用鼠标框选卡号区域
4. 用鼠标框选密码区域
5. 保存模板
6. 点击 **"开始识别"** 使用模板识别

### 3. 编辑结果

- 查看识别结果
- 如有错误，手动修改卡号或密码
- 点击 **"重新识别"** 重新识别当前图片

### 4. 导出结果

**导出文档**
- 点击 **"导出文档"**
- 生成文本文件，包含所有卡号和密码

**导出Excel（推荐）**
- 点击 **"导出Excel"**
- 生成Excel文件，包含：
  - 序号
  - 卡号
  - 密码
  - 密码图片（每行一个图片）

---

## 🛠️ 管理服务

### 启动服务
```batch
双击：start.bat
```

### 停止服务
```batch
双击：stop.bat
```

### 检查状态
```batch
双击：check.bat
```

### 查看日志
```batch
type logs\backend.log
type logs\frontend.log
```

---

## ⚠️ 常见问题

### Q1: 双击 install.bat 没反应

**A**:
1. 右键点击 `install.bat`
2. 选择 **"以管理员身份运行"**
3. 如果还不行，按住 `Shift` 键，右键点击 `install.bat`
4. 选择 **"以管理员身份运行"**

---

### Q2: 安装失败，提示"Python未安装"

**A**:
1. 访问：https://www.python.org/downloads/release/python-3128/
2. 下载 **"Windows installer (64-bit)"**
3. 运行安装程序
4. **重要**：勾选 **"Add Python to PATH"**
5. 点击 "Install Now"
6. 安装完成后，重新运行 `install.bat`

---

### Q3: 端口5000或8001被占用

**A**:
1. 双击 `stop.bat` 停止现有服务
2. 如果还不行，找到占用端口的进程并关闭：

```batch
# 查看占用5000端口的进程
netstat -ano | findstr :5000

# 结束进程（将PID替换为实际进程ID）
taskkill /PID 进程ID /F
```

3. 重新双击 `start.bat`

---

### Q4: 首次启动很慢

**A**: **正常现象**

首次启动需要：
- 下载OCR模型文件（约200MB）
- 加载模型到内存
- 启动Python和Node.js服务

**时间**：
- 首次启动：30-60秒
- 后续启动：10-20秒

---

### Q5: 识别结果不准确

**A**:
1. **使用模板识别**（推荐）：设置识别模板，框选卡号和密码区域
2. **提高图片质量**：使用高清图片，避免模糊
3. **确保光线充足**：拍照时光线要好
4. **调整图片角度**：确保卡片水平放置

---

### Q6: 看不到密码图片

**A**:
1. 检查图片是否正确上传
2. 点击密码框上方的图片预览
3. 点击 **"重新识别"** 重新提取密码图片

---

### Q7: Excel导出失败

**A**:
1. 确保Excel文件未打开
2. 检查是否有写入权限
3. 尝试导出到其他位置（如桌面）

---

### Q8: 服务启动失败

**A**:
1. 双击 `check.bat` 查看服务状态
2. 查看日志文件：
   ```batch
   type logs\backend.log
   type logs\frontend.log
   ```
3. 检查Python和Node.js是否正确安装：
   ```batch
   python --version
   node --version
   ```
4. 重新运行 `install.bat`

---

## 🔧 高级选项（可选）

### 修改端口

编辑 `.env` 文件：
```env
FRONTEND_PORT=5000
BACKEND_PORT=8001
```

### 修改OCR模型

编辑 `backend/ocr_service.py`：
```python
# 使用不同模型
ocr = PaddleOCR(use_angle_cls=True, lang='ch')  # 中文
ocr = PaddleOCR(use_angle_cls=True, lang='en')  # 英文
```

### 开启调试模式

编辑 `start.bat`：
```batch
set DEBUG=True
```

---

## 📞 获取帮助

### 查看文档
- 完整部署指南：`README.md`
- 便携式Python指南：`PORTABLE_PYTHON.md`
- PaddleOCR文档：`PADDLEOCR_DEPLOY.md`

### 检查服务状态
```batch
双击：check.bat
```

### 手动检查
```batch
# 检查后端
curl http://localhost:8001/health

# 检查前端
curl http://localhost:5000
```

---

## 🎉 完成！

现在您可以：

- ✅ 上传卡片图片自动识别
- ✅ 导出识别结果到Excel
- ✅ 完全离线运行，无需联网
- ✅ 数据安全，本地处理
- ✅ 支持批量识别，提高效率

**访问地址：http://localhost:5000**

---

## 💡 小贴士

### 优化识别准确率
1. 使用高清图片
2. 确保卡片水平放置
3. 光线充足
4. 使用模板识别模式

### 提高识别速度
1. 减少同时上传的图片数量（建议5张以内）
2. 使用模板识别模式（比自动识别快）
3. 关闭不必要的程序释放内存

### 确保数据安全
1. 定期备份识别结果
2. 不要在公共电脑上使用
3. 识别完成后及时删除原始图片

---

## 🚀 下一步

1. **设置识别模板**（提高准确率）
2. **批量识别**（提高效率）
3. **导出结果**（整理数据）
4. **分享给同事**（提高团队效率）

---

## 📊 其他方案对比

| 方案 | 时间 | 难度 | 优点 | 缺点 |
|------|------|------|------|------|
| **标准部署** | 10分钟 | ⭐⭐ | 无需Docker，自动安装依赖 | 首次安装稍慢 |
| Docker部署 | 5分钟 | ⭐ | 一键启动，环境隔离 | 需要安装Docker |
| 便携式部署 | 15分钟 | ⭐⭐⭐⭐ | 不影响系统环境 | 配置复杂 |

---

**推荐使用标准部署，简单快捷！**
